---
import { Image } from 'astro:assets';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/sections/es/es_Footer.astro';
import Header from '../../components/sections/Header.astro';


import WorkExperience from '@/components/sections/WorkExperience.astro';
import { work } from '../../data/es/es_work';

import Studies from '@/components/sections/Studies.astro';
import { studies } from '../../data/es/es_studies'

import GlowLineSeparator from '@/components/ui/separator/GlowLineSeparator.astro';
import ArrowSeparator from '../../components/ui/separator/ArrowSeparator.astro'

type Props = {
  title: string;
  description: string;
  showHeader?: boolean;
  heroImage?: ImageMetadata;
};

const { title, description, showHeader = true, heroImage } = Astro.props as Props;

---

<html lang="en" data-theme="dark" class="dark">
	<head>
		<BaseHead title={title} description={description} image={heroImage} />
		<meta property="og:type" content="article" />

		<style>
				/* Align blog layout with site container to avoid right overflow and tight column */
				main {
					width: 1280px;
					max-width: calc(100% - 2em);
					margin: auto;
					background: var(--color-bg-primary);
				}
			.hero-image {
				width: 100%;
			}
			.hero-image img {
				display: block;
				margin: 0 auto;
				border-radius: 12px;
				box-shadow: var(--box-shadow);
			}
				.prose {
					width: 100%;
					max-width: 870px; /* widen column */
					margin: auto;
					padding: 0.05em;
					color: var(--color-text-secondary);
				}
            .title {
				margin-bottom: 1em;
				padding: 1em 0;
				text-align: center;
				line-height: 1;
			}
			.title h1 {
                margin: 0 0 0.5em 0;
                color: var(--color-text-primary);
			}
			.date {
                margin-bottom: 0.5em;
                color: var(--color-text-muted);
			}
			.last-updated-on {
				font-style: italic;
			}
		</style>
	</head>

	<body>
		<Header />
		<main>
			<article>
			{showHeader && (
				<div class="hero-image">
				{heroImage && <Image width={1020} height={510} src={heroImage} alt="" />}
				</div>
			)}
			<div class="prose">
				<p style="text-align:center; font-weight:bold; margin-top:1rem;">
				</p>
				<slot />
			</div>
			<section class="w-full py-6 md:py-10">
				<div class="mx-auto max-w-5xl px-4">
				<div class="flex flex-col md:flex-row items-center gap-6 rounded-2xl p-6 border-2 border-[var(--accent)] bg-[var(--color-bg-primary)]">

				<!-- Imagen -->
				<img 
					src="/portfolio_astro_page/img1.png" 
					alt="Nahuel Aparicio Del Blanco" 
					class="w-40 h-40 md:w-40 md:h-40 rounded-full object-cover shadow-md"
				/>

				<!-- Texto -->
				<div class="text-center">
					<h1 class="text-1xl md:text-5xl font-extrabold tracking-tight text-foreground">
					<span class="bg-gradient-to-r from-[var(--accent)] to-[var(--accent-dark)] bg-clip-text text-transparent">
						Nahuel Aparicio Del Blanco
					</span>
					</h1>
					<p class="mt-2 text-lg md:text-xl text-[color:var(--color-text-secondary)]">
					Programador de Gameplay y Desarrollador de Videojuegos – Barcelona, España.
					</p>
				</div>

				</div>
			</div>
		</section>
			<p>
				Desde que era pequeño me ha fascinado cómo los videojuegos crean mundos llenos de vida. 
				Esa curiosidad se transformó en una pasión por el <b>desarrollo de videojuegos</b> y la <b>programación de gameplay</b>. 
				Actualmente, me concentro en construir <b>sistemas eficientes, modulares y divertidos de jugar</b>.
			</p>
			<p>
				Me considero con gran capacidad de <b>resolución de problemas</b>, siempre buscando escribir código escalable y abierto a feedback constructivo 
				que me ayude a mejorar tanto personal como profesionalmente.
			</p>
			<p>
				Comencé a programar en <b>2017</b>, enfocándome principalmente en <b>Unity (C#)</b>, mientras adquiría experiencia 
				con <b>C++</b> y motores/frameworks como Unreal, OpenGL, SFML y Phaser.
			</p>
			<p>
				Mi objetivo final es programar <b>sistemas de juego</b> que den vida a ideas y diseños, 
				combinando precisión técnica con la magia y diversion del juego.
			</p>

			<ArrowSeparator/>
			</article>
		</main>
		<GlowLineSeparator/>
		<Studies items={studies} title="Estudios"/>
		<GlowLineSeparator/>
		<WorkExperience items={work} title="Experiencia Laboral" />
		<Footer />
	</body>
</html>
